import 'package:flutter/material.dart';

/// Phase 0 placeholder. Phase 2 replaces with in/out CRUD + datetime + category picker.
class TransactionsScreen extends StatelessWidget {
  const TransactionsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Transactions — Phase 2 builds entry UI here')));
  }
}
